# Runtime dependencies.
PyYAML>=6.0     # parses data/control-mappings.yaml at runtime (miccmac/metadata.py, miccmac/config.py)
paramiko>=3.0   # SSH transport for miccmac/connectors/ssh_osquery.py
# Add packages here as you implement real check logic, for example:
#   requests      # if a check queries an MDM / SIEM / inventory API
